The repository contains Deep Learning models that have been trained on real life problems

The repository contains differnet models that are trained and saved in notebooks, just download the notebooks and open on any jupyter notebook, the dataset can be found inside the notebooks
